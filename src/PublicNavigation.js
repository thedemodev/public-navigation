import React from 'react';

import PublicNavigationWithoutContent from './PublicNavigationWithoutContent';

const PublicNavigation = props => <PublicNavigationWithoutContent {...props} />;

export default PublicNavigation;
